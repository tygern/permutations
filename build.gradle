buildscript {
    ext {
        kotlinVersion = "1.1.0"
        aspenVersion = "2.0.0"
    }

    repositories {
        jcenter()
        mavenCentral()
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
    }
}